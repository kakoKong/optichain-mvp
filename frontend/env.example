# Development Environment Configuration
# Copy this file to .env.local and modify as needed

# Enable development mode (bypasses LINE authentication)
NEXT_PUBLIC_DEV_MODE=true

# Supabase Configuration (required)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# LINE LIFF Configuration (optional in dev mode)
NEXT_PUBLIC_LINE_LIFF_ID=your_line_liff_id

# LINE Browser OAuth Configuration (for non-LIFF LINE login)
NEXT_PUBLIC_LINE_CLIENT_ID=your_line_channel_id
LINE_CLIENT_SECRET=your_line_channel_secret

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Development Notes:
# - Set NEXT_PUBLIC_DEV_MODE=true to enable dev authentication
# - Dev mode automatically activates on localhost
# - You can still use LINE authentication by setting NEXT_PUBLIC_DEV_MODE=false
# - Dev users get a unique ID like "dev_username_timestamp"
